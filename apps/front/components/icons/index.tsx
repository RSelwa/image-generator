import { StripVertical, StripHorizontal} from "@/components/icons/strip-vertical"
import { ColoredGoogleIcon, GoogleIcon } from "./google"
import LoaderIcon from "./loader"
import { Logo as LogoIcon, NewLogo as NewLogoIcon, LogoWithIcon } from "./logo"

export { ColoredGoogleIcon, GoogleIcon, LoaderIcon, LogoIcon, NewLogoIcon, StripVertical, LogoWithIcon, StripHorizontal }
