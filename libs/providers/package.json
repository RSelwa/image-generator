{
  "name": "@repo/providers",
  "type": "module",
  "version": "0.0.0",
  "description": "Providers of services for the monorepo",
  "license": "MIT",
  "exports": {
    "./db-refs": {
      "types": "./dist/db-refs.js",
      "default": "./dist/db-refs.js"
    },
    "./firebase": {
      "types": "./dist/firebase.js",
      "default": "./dist/firebase.js"
    },
    "./upload-post": {
      "types": "./dist/upload-post.js",
      "default": "./dist/upload-post.js"
    }
  },
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "compile": "tsc --noEmit",
    "lint": "biome check",
    "check-circular": "madge --circular --extensions ts ./src --ts-config ./tsconfig.json",
    "build": "tsdown",
    "coverage": "vitest run --coverage",
    "clean": "del-cli dist isolate tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "@opensearch-project/opensearch": "^3.5.1",
    "@repo/common": "workspace:*",
    "@repo/schemas": "workspace:*",
    "dotenv": "^17.2.3",
    "firebase": "^12.3.0",
    "firebase-admin": "^13.5.0",
    "firebase-functions": "^6.3.0",
    "stripe": "^9.16.0",
    "upload-post": "^2.0.4"
  },
  "devDependencies": {
    "@codecompose/typescript-config": "^2.2.0",
    "@types/node": "^22.10.10",
    "del-cli": "^7.0.0",
    "madge": "^8.0.0",
    "tsdown": "^0.15.6",
    "typescript": "^5.7.3",
    "vitest": "^3.2.4"
  }
}
