{
  "name": "@repo/schemas",
  "description": "Schemas used across the repo",
  "version": "1.0.0",
  "type": "module",
  "files": ["dist"],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsdown",
    "compile": "tsc --noEmit",
    "lint": "biome check",
    "check-circular": "madge --circular --extensions ts ./src --ts-config ./tsconfig.json",
    "test": "vitest run --coverage",
    "clean": "del-cli dist isolate tsconfig.tsbuildinfo",
    "coverage": "vitest run --coverage"
  },
  "license": "MIT",
  "dependencies": {
    "@firebase/firestore": "^4.10.0",
    "@repo/common": "workspace:*",
    "firebase": "^12.8.0",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@codecompose/typescript-config": "^2.2.0",
    "del-cli": "^7.0.0",
    "madge": "^8.0.0",
    "tsdown": "^0.15.6",
    "typescript": "^5.7.3",
    "vite": "^7.1.7",
    "vitest": "^3.2.4"
  }
}
