{
  "name": "@repo/http",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "files": ["dist", ".env.*"],
  "scripts": {
    "compile": "tsc --noEmit",
    "lint": "biome check",
    "check-circular": "madge --circular --extensions ts ./src --ts-config ./tsconfig.json",
    "test": "vitest run --coverage",
    "build": "tsdown",
    "clean": "del-cli dist isolate tsconfig.tsbuildinfo coverage",
    "test:coverage": "vitest run --coverage",
    "deploy": "firebase deploy --project pusher-292200",
    "emulate": "firebase emulators:start --project pusher-292200",
    "login": "firebase login",
    "env": "ls env.txt && grep -Ev '^#' env.txt  | xargs --no-run-if-empty -I NAME sh -c 'echo NAME=\"$(firebase functions:secrets:access \"$1\")\" >> .env.local' _ NAME || echo \"Please provide an env.txt\""
  },
  "license": "MIT",
  "dependencies": {
    "@google-cloud/functions-framework": "^4.0.0",
    "@repo/common": "workspace:*",
    "@repo/providers": "workspace:*",
    "cors": "^2.8.5",
    "firebase-admin": "^13.5.0",
    "firebase-functions": "^6.4.0",
    "nocache": "^4.0.0"
  },
  "devDependencies": {
    "@codecompose/typescript-config": "^2.2.0",
    "@types/cors": "^2.8.19",
    "@types/node": "^22.10.10",
    "@vitest/coverage-v8": "^3.2.4",
    "del-cli": "^6.0.0",
    "firebase-functions-test": "^3.4.1",
    "madge": "^8.0.0",
    "tsdown": "^0.15.6",
    "typescript": "^5.9.0",
    "vite": "^7.1.7",
    "vitest": "^3.2.4"
  }
}
