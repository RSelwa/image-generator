{
  "rules": {
    "lobbies": {
      "$lobbyId": {
        "players": {
          "$userId": {
            ".read": "auth != null",
            ".write": "auth != null && auth.uid === $userId"
          }
        }
      }
    }
  }
}
